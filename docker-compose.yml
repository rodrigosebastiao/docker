services:
  nginx:
    profiles: [ "infra" ] # Select this group with docker compose
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # - ./conf.d:/etc/nginx/conf.d
      # - ./certbot/www:/var/www/certbot
      # - ./certbot/conf:/etc/letsencrypt
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./certbot:/etc/letsencrypt
    networks:
      - web
      - internal

  postgres:
    profiles: [ "infra" ] # Select this group with docker compose
    image: postgres:17
    container_name: postgres
    environment:
      POSTGRES_DB: strapi
      POSTGRES_USER: strapi_db_user
      POSTGRES_PASSWORD: "!Asder2312"
    # ports:
    #   - "5432:5432" # Expose external
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal

  names-backend:
    profiles: [ "apps" ] # Select this group with docker compose
    build: ../names-backend
    networks:
      - internal

  stocks-backend:
    profiles: [ "apps" ] # Select this group with docker compose
    build: ../stocks-backend
    networks:
      - internal

networks:
  web:
    external: true
  internal:


volumes:
  pgdata:
